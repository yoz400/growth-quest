# CURRENT

更新日: 2026-08-24

> ⚠ 60行以内。履歴は書かない。「今どうなっているか」だけ。
> 上限を超えたら、書き足すのではなく減らす。履歴は DECISIONS.md へ。

## 最重要目標

**ヨージの実機で一度通しで触り、今回の改善が本当に効いているか確かめる。**
チーム監査のフェーズ1〜5をほぼ消化したが、実機検証は**一度も行われていない**
（roadmap §5）。いまの「効いているはず」はコードからの推定にすぎない。

## 進行中（最大3件）

### 1. スマホが古い版のまま問題への対策（guild-171）
状態: 実装・ブラウザ検証済み。`version.json`（bump_version.sh が index.html と同時に
      書き出す）を再開時に見に行き、番号違いなら画面下に更新の帯を出す。
      sw.js はHTML取得を `cache:'reload'` にし、CACHE_NAME を v16 へ。
      設定「Growth Quest について」に現在のビルド番号を表示
次: iPhoneで確認。**SWはプレビューで登録できず未検証**（本番でしか確かめられない）

### 2. カレンダー3件（guild-167／相棒の修正が guild-172）
状態: ①🪶鳳凰の羽 ②🛡守りの盾 の守った日に「守」マーク（🪶橙／🛡青。history には
      書かない＝学習日数は増えない）③完璧な週の相棒を手持ちオトモン最大4体へ。
      ③は起動時 window.Otomon 未読込で妖精のまま固定される不具合を guild-172 で修正
次: ヨージの実機で確認（①②は未確認、③は妖精のままだった→修正後の再確認待ち）

### 3. すごろくのマスの言葉 ／ フェーズ5の残り
状態: 文言は未着手。`tests.html` 未作成。台帳は `gq_guide_tutorial_seen` 未記載・
      件数表記 50→55 に要修正
次: 文言は判断基準1で見直す。台帳とtestsは手が空いたときに

## Today's Focus（最大3件）

1. 2.  3.

## Blockers

- なし（名言の存廃は 2026-08-23 に決着。♡・バッジ・称号を撤去し名言は残す）

## AIへの申し送り

- **作業のたびに CURRENT.md を更新する**
- **装備・響く言葉はヨージが意図して撤去済み。復活させない**（装備=画像未完成で
  アバターが崩れる／響く言葉=未出会いの言葉までネタバレする作り）
- 「使われていない」という指摘は、**git履歴 → Obsidian Vault → 本人**の順で裏取り
  してから動く。繋ぎ忘れと意図的な撤去はコードからは見分けられない（FAILURES）
- 機能の採否は roadmap の**4基準**で判断する（努力を要求しない報酬は与えない 他）
- **同じ animation を複数要素に付けるときは軌道と静止位置をずらす**（FAILURES 参照）
- **検証で `renderX()` を手で呼ばない**。アプリが自力で描いたDOMを見る。
  手で呼ぶと、起動時の読み込み順の穴（otomon.js は最後）を見逃す（FAILURES 08-24）
- **OverlayManager の外にいる浮き要素に注意**（body直下・position:fixed）。
  モーダルが開くと inert を浴び「見えているのに押せない」状態で固まる。
  操作部品を持つのは `#review-prompt`・`#cal-day-popup`・`#update-bar` の3つ。
  **出す瞬間に閉じているかを1回見るだけでは不足**（あとから開くほうが多い）。
  `#update-bar` のように定期的に見張って出し入れすること（FAILURES 2026-08-23）

## 本番の状態

最新バージョン: guild-172（`version.json` も同じ番号か必ず確認）
デプロイ状態: 正常（main と origin/main は同期済み・未コミットなし）
