# CURRENT

更新日: 2026-08-23

> ⚠ 60行以内。履歴は書かない。「今どうなっているか」だけ。
> 上限を超えたら、書き足すのではなく減らす。履歴は DECISIONS.md へ。

## 最重要目標

**ヨージの実機で一度通しで触り、今回の改善が本当に効いているか確かめる。**
チーム監査のフェーズ1〜5をほぼ消化したが、実機検証は**一度も行われていない**
（担当がセッション上限で中断。roadmap §5）。いまの「効いているはず」は
すべてコードからの推定であり、確かめた事実ではない。

## 進行中（最大3件）

### 1. オトモン図鑑の詳細ビュー（guild-161・デプロイ済み）
状態: 実装・ブラウザ検証済み。ヨージの実機で guild-160 の動作確認まで完了
次: guild-161 で足した詳細内の「🤝 ふれあう」を実機で触ってもらう

### 2. すごろくのマスの言葉を検討
状態: 未着手。「1から順に」の3つ目（1=safeSetItem・2=振るか選べる は完了）
次: 100マスの文言を、判断基準1「努力を要求しない報酬を与えない」で見直す

### 3. フェーズ5 安全網の残り ／ localStorage 台帳
状態: safeSetItem 完了（guild-151）。`tests.html` 未作成。台帳は `gq_guide_tutorial_seen`
      1件が未記載、件数表記も 50→55 に要修正
次: どちらも小。手が空いたときに片付ける

## Today's Focus（最大3件）

1.
2.
3.

## Blockers

- なし（名言の存廃は 2026-08-23 に決着。♡・バッジ・称号を撤去し名言は残す）

## AIへの申し送り

- `docs/AI/` は 2026-08-08 に運用開始。**作業のたびに CURRENT.md を更新する**
- **装備・響く言葉はヨージが意図して撤去済み。復活させない**
  （装備=画像未完成でアバターが崩れる／響く言葉=未出会いの言葉までネタバレする作り）
- 「使われていない」という指摘は、**git履歴 → Obsidian Vault → 本人**の順で
  意図を裏取りしてから動く。コードから分かるのは「呼ばれていない」だけで、
  繋ぎ忘れと意図的な撤去は見た目が同じ（FAILURES.md 参照）
- 機能の採否は roadmap の**4基準**で判断する（努力を要求しない報酬は与えない 他）
- **OverlayManager の外にいる浮き要素に注意**（body直下・position:fixed）。
  モーダルが開くと inert を浴び「見えているのに押せない」状態で固まる。
  操作部品を持つのは `#review-prompt`（対策済 guild-163）と `#cal-day-popup` の2つ。
  新たに body 直下へ固定表示を足すなら、必ずモーダル中は出さないこと

## 本番の状態

最新バージョン: guild-163
デプロイ状態: 正常（main と origin/main は同期済み・未コミットなし）
