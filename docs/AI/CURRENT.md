# CURRENT

更新日: 2026-08-26

> ⚠ 60行以内。履歴は書かない。「今どうなっているか」だけ。
> 上限を超えたら、書き足すのではなく減らす。履歴は DECISIONS.md へ。

## 最重要目標

**ヨージの実機で一度通しで触り、改善が本当に効いているか確かめる。**
フェーズ1〜5をほぼ消化したが、実機検証は一度も行われていない（roadmap §5）。
いまの「効いているはず」は、コードからの推定にすぎない。

## 進行中（最大3件）

### 1. 実機確認の積み残し
状態: 下記すべてブラウザ検証は済み・**実機は未確認**
      ・guild-171 古い版のまま問題（SWはプレビューで登録できず本番でしか確かめられない）
      ・guild-167〜173 カレンダーの「守」マーク・完璧な週の相棒
      ・guild-175 レビュー案内の「開く」が週タブで開くか（日曜夜/月曜しか踏めない）
次: iPhoneで通しで触る

### 2. すごろくのマスの言葉（文言待ち）
状態: **仕組みは完成（guild-176）**。areas.js に `AREA_LINES`（10エリアの空枠）と
      `lineFor()` を用意。空なら従来文言に落ちるので、埋めた分から順に反映される
次: ChatGPTに10エリア分（lines 3つ＋clear 1つ）を書いてもらい、貼ってもらう

### 3. フェーズ5の残り
状態: `tests.html` 未作成。台帳は `gq_guide_tutorial_seen` 未記載・件数 50→55 要修正
次: どちらも小。手が空いたときに

## Today's Focus（最大3件）

1. 2. 3.

## Blockers

なし

## AIへの申し送り

- **作業のたびに CURRENT.md を更新する**
- **装備・響く言葉はヨージが意図して撤去済み。復活させない**（装備=画像未完成で
  アバターが崩れる／響く言葉=未出会いの言葉までネタバレする作り）
- 「使われていない」指摘は **git履歴 → Vault → 本人** の順で裏取りしてから動く。
  繋ぎ忘れと意図的な撤去は、コードからは見分けられない（FAILURES）
- 機能の採否は roadmap の**4基準**で判断する（努力を要求しない報酬は与えない 他）
- **OverlayManager の外にいる浮き要素に注意**（body直下・position:fixed）。
  モーダルが開くと inert を浴び「見えているのに押せない」状態で固まる。
  該当は `#review-prompt`・`#update-bar`（どちらも見張り方式で対策済）と
  `#cal-day-popup`（現状モーダル内から開く経路が無いので未対策）。
  **出す瞬間に1回見るだけでは不足**。定期的に見張ること（FAILURES 08-23/08-26）
- **画面に固定表示するものは、位置を実測して置く**。ヘッダーの高さは端末幅と
  ボタンの折り返しで変わるので、CSSの固定値だと必ずどこかで覆う（guild-177）
- その他の再発防止（FAILURES 参照）: 同じanimationは軌道をずらす／検証で
  `renderX()` を手で呼ばない／`% n` は n を1〜12すべて試す

## 本番の状態

最新バージョン: guild-178（`version.json` も同じ番号か必ず確認）
デプロイ状態: 正常（main と origin/main は同期済み・未コミットなし）
