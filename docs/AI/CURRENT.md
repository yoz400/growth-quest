# CURRENT

更新日: 2026-09-08

> ⚠ 60行以内。履歴は書かない。「今どうなっているか」だけ。
> 上限を超えたら、書き足すのではなく減らす。履歴は DECISIONS.md へ。

## 最重要目標

**ヨージの実機で一度通しで触り、改善が本当に効いているか確かめる。**
フェーズ1〜5をほぼ消化したが、実機検証は一度も行われていない（roadmap §5）。
いまの「効いているはず」は、コードからの推定にすぎない。

## 進行中（最大3件）

### 1. ⚠️ 未コミットで止まっている作業（2026-09-04から動いていない）
状態: `scripts/core.js` に **🏮導きの灯**（findGuideTarget 等）が作りかけで残る。
      `docs/architecture_review.md`・`tests.html`（フェーズ5の安全網）も未コミット。
      **JSを触るときは自分のパスだけ `git add` すること**
次: 灯籠を仕上げるか退避するかをヨージが決める

### 2. 実機確認の積み残し
状態: ブラウザ検証は済み・**実機は未確認**
      ・guild-171 古い版のまま問題（SWはプレビューで登録できず本番でしか確かめられない）
      ・guild-167〜173 カレンダーの「守」マーク・完璧な週の相棒
      ・guild-175 レビュー案内の「開く」が週タブで開くか（日曜夜/月曜しか踏めない）
      ・guild-179〜181 バッジ通知の位置／チケット上限の注意／ジャンルの折りたたみ
      ・guild-185 卵から前と違う子が孵るか
次: iPhoneで通しで触る

### 3. すごろくのマスの言葉（文言待ち）
状態: **仕組みは完成（guild-176）**。areas.js の `AREA_LINES` は10エリアとも空。
      空なら従来文言に落ちるので、埋めた分から順に反映される
次: ChatGPTに10エリア分（lines 3つ＋clear 1つ）を書いてもらい、貼ってもらう

## Today's Focus（最大3件）

1. 2. 3.

## Blockers

なし

## AIへの申し送り

- **作業のたびに CURRENT.md を更新する**
- **装備・響く言葉はヨージが意図して撤去済み。復活させない**（装備=画像未完成でアバターが崩れる／響く言葉=ネタバレする作り）
- 「使われていない」指摘は **git履歴→Vault→本人** の順で裏取りする（繋ぎ忘れと意図的な撤去は見分けられない）
- 機能の採否は roadmap の**4基準**で判断する（努力を要求しない報酬は与えない 他）
- **body直下・position:fixed の浮き要素に注意**。モーダルが開くと inert を浴び「見えているのに
  押せない」で固まる。**1回見るだけでは不足＝定期的に見張る**。未対策=`#cal-day-popup`。置き場所は `topBelowHeader()`
- **可変の量を持つUIは実データ相当まで増やして測る**（ジャンル1個で測り小画面でSTART画面外・guild-181）
- **抽選プールは「所持済みを除く」を必ず考える**（卵から同じ子ばかり孵っていた・guild-185）
- **データはソースから切り出さず、動いているモジュールから取る**（FAILURES 09-08）
- その他（FAILURES）: animationは軌道をずらす／`renderX()`を手で呼ばない／`% n`は1〜12全部試す

## 本番の状態

最新バージョン: guild-185（`version.json` も同じ番号か必ず確認）
デプロイ状態: 正常。**ただし未コミットあり**（「進行中1」を参照）
