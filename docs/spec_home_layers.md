# 仕様書：ホームの整理（ロードマップ フェーズ4）

作成: 2026-08-03 ／ 設計: クロ（Opus 5）／ 実装担当: Codex
親: [team/roadmap.md](team/roadmap.md) フェーズ4 ／ [architecture_review.md](architecture_review.md) §4 掟
⚠️ 他のJS作業と同時進行しない。**H-1 → H-2 → H-3 の順**。各段階でコミットを分ける。

---

## 0. 先に：報告されていた4件を実機で検証した（2026-08-03）

チームの監査レポート（2026-07-27）から1週間で、フェーズ1〜3の作業が家の中を触っている。
**着手前に4件が今も実在するかを確認した。1件は既に直っていた。**

| # | 報告内容 | 検証結果 |
|---|---|---|
| 1 | 並べ替えの `KNOWN_IDS` に幽霊エントリ `xp-panel` `mode-panel` | ❌ **既に修正済み**（[boot.js:1234](../scripts/boot.js:1234) に経緯のコメントあり）。`app.css` に使われないCSSだけ残っている（実害なし） |
| 2 | otomonの3枚が並べ替え対象外で取り残される | ⚠️ **症状が違う。より広く、より悪い**（§1） |
| 3 | ヘッダー8ボタンが375px端末で潰れる | ✅ **実在**。1つ **34 x 40px**（推奨44x44） |
| 4 | ロック中ナビのコントラストがAA未満 | ✅ **実在**。**2.16 : 1**（AA基準4.5）・文字 **7.68px** |

> 💡 「報告されているから直す」ではなく、**測ってから直す**。1件は徒労になるところだった。

---

## 1. H-1：並べ替えがカードを置き去りにする（最優先・バグ修正）

### 何が起きるか

ウィジェットの並べ替えは `.widget` クラスの付いた**7枚だけ**を対象にしている
（[boot.js:1243](../scripts/boot.js:1243) `getWidgets()`）。ホームにはそれ以外にも
カードがあり、**並べ替えるとその相対位置が壊れる**。

```text
【並べ替え前】                    【timer を先頭にして再訪したあと】
  ジャンル                          はじめの一歩      ← 置き去り
  ⏱ タイマー                        今日の予定        ← 置き去り
  はじめの一歩                      ⏱ タイマー        ← ユーザーが先頭にしたはず
  今日のクエスト                    ジャンル
  🥚卵 / 🤝お供 / ⚡クエスト        今日のクエスト
  使命                              🥚卵 / 🤝お供 / ⚡クエスト
  今日の予定                        使命
  統計 / 打刻 / カレンダー          統計 / 打刻 / カレンダー
```

原因は `applyOrder` が **既知の7枚を `app.appendChild()` で末尾へ送り直す**方式だから
（[boot.js:1274-1277](../scripts/boot.js:1274)）。対象外のカードはその場に残るので、
結果として**全部の上に浮き上がる**。

### なぜ重大か

**ロードマップ フェーズ1-1「STARTを画面内へ」を打ち消す**。
ユーザーがタイマーを先頭に持ってきても、対象外カードが上に割り込む。

実測（375x812・並べ替え後・両カードが表示状態のとき）:

```text
はじめの一歩   上端 192px
今日の予定     上端 351px
⏱ タイマー     上端 432px   ← 先頭にしたのに3番目
START          上端 703px   （画面の高さ 812px）
```

### 対象外になっているカード（全5枚）

| id | 出どころ | クラス |
|---|---|---|
| `onboard-card` | [index.html:168](../index.html:168) | `glass`（`widget` 無し） |
| `today-plan-card` | 手帳S3-2が生成 | `glass` |
| `otomon-egg-card` | [otomon.js:1228](../scripts/otomon.js:1228) | `glass` |
| `otomon-buddy-card` | [otomon.js:1466](../scripts/otomon.js:1466) | `glass` |
| `otomon-quest-card` | [otomon.js:1533](../scripts/otomon.js:1533) | `glass` |

### 直し方（2案。**A案を推奨**）

**A案：並べ替えを「動かさないカード」に配慮する形へ変える**

`applyOrder` を「末尾へ送り直す」から「**既知ウィジェットの並びだけを入れ替える**」に変える。
対象外カードは**元の位置（DOM上の前後関係）を保つ**。

```javascript
// 変更前：既知の7枚を末尾へ送り直す＝対象外カードが上に取り残される
for (const id of orderedIds) {
  const el = document.getElementById(id);
  if (el) app.appendChild(el);
}

// 変更後：いま .widget が占めている「席」だけを並べ替える。
//         対象外カードの位置は動かさないので、上に浮き上がらない。
const slots = getWidgets();                        // いまの .widget の出現位置＝席
const cards = orderedIds.map(id => document.getElementById(id)).filter(Boolean);
slots.forEach((slot, i) => {
  if (cards[i] && cards[i] !== slot) app.insertBefore(cards[i], slot);
});
```

> ⚠️ 席の数とカードの数がズレる場合（未解放で非表示のカードなど）に備え、
> `slots.length !== cards.length` のときは**並べ替えを諦めてデフォルト順のままにする**。
> 中途半端に動かすほうが壊れて見える。

**B案：対象外カードにも `widget` クラスを付けて並べ替え対象にする**

一見きれいだが、**採用しない**。理由は3つ:
- `onboard-card` は「はじめの一歩」で、**新規ユーザーにだけ出て自動で消える**。
  並べ替えの対象にすると「消えたカードの席」が保存に残る
- otomonの3枚は `daily-quest-card` の直後に置くことに意味がある（クエストと並べて見せる設計）
- 保存キー `gq_widget_order` の中身が変わるので、**既存ユーザーの並びが1回壊れる**

### H-1 の受け入れ基準

- [ ] タイマーを先頭に並べ替えて**リロードしても**、タイマーがいちばん上にある
- [ ] `はじめの一歩` `今日の予定` `オトモン3枚` が、並べ替え後もそれぞれ元の前後関係を保つ
      （オトモン3枚は `今日のクエスト` の直後のまま）
- [ ] 並べ替えのリセットボタンでデフォルト順に戻る
- [ ] 375px で START が初回表示の画面内（上端 812px 以内）に収まる
- [ ] コンソールエラーゼロ／`bash tools/bump_version.sh` 実行済み

---

## 2. H-2：ヘッダーの操作性（8ボタン）

### 実測（375x812）

```text
ボタン8個  幅34px × 高40px       ← 推奨は44x44
ロック中のラベル  2.16 : 1        ← AA基準は4.5 : 1
ロック中の文字     7.68px          ← 小さすぎて読めない
解放済みのラベル  5.45 : 1        ← こちらは基準を満たしている
```

ロック中が読めないのは**二重に薄くしているから**。
ボタン全体に `opacity: .5`、その中のラベルに `rgba(255,255,255,.48)`
（[app.css:4177-4186](../styles/app.css:4177)）→ 実効 0.24。

### 直し方

1. **二重の薄さをやめる**。`opacity: .5` を外し、ラベル色だけで濃さを決める。
   `rgba(255,255,255,.62)` 程度にすると **4.5 : 1 を超える**（実装後に必ず測ること）
2. **文字を大きくする**。7.68px → 最低 10px
3. **タップ領域を 44x44 に**。見た目の幅は変えず `padding` と `min-height` で確保する
   （8個 × 44px = 352px。375px 端末なら横並びのまま収まる）

> ⚠️ ロック中を「読めるようにする」のであって「解放済みと同じ濃さにする」のではない。
> **まだ使えないことは一目で分かる必要がある**（フェーズ0で「シルエット＋？？？」に
> した意図を壊さないこと）。差は色の濃さではなく、**グレースケール**で付ける。

### H-2 の受け入れ基準

- [ ] ロック中ラベルのコントラストが **4.5 : 1 以上**（測って数値を報告すること）
- [ ] ロック中の文字が **10px 以上**
- [ ] 各ボタンのタップ領域が **44 x 44px 以上**
- [ ] 320px 幅でもヘッダーが横スクロールしない
- [ ] ロック中と解放済みが一目で見分けられる（グレースケールで差を付ける）
- [ ] コンソールエラーゼロ／`bump_version.sh` 実行済み

---

## 3. H-3：三層化（⚠️ ヨージの判断が要る。まだ着手しない）

ロードマップの本題。ホームの最大14枚を「いま／きょう／ふりかえり」の3層にまとめる案。

```text
【いま】フラット14層          【提案】3層
  …14枚が縦に並ぶ               🎯 いま        ← タイマー（常に最上段）
  優先度の差が視覚的にゼロ      📋 きょう      ← クエスト/予定/打刻をタブで1枚に
                                📚 ふりかえり  ← 統計/カレンダー
```

**これは仕様書だけでは決められない。** 理由は、3層に畳むと
**いくつかのカードがタブの裏に隠れる**からで、どれを隠してよいかはヨージにしか分からない。

### ヨージに決めてもらうこと

1. **「きょう」タブに何を入れるか**（クエスト／今日の予定／打刻／使命／オトモン3枚）
2. **タブを開いた状態を覚えるか**（覚えると「いつも同じタブしか見ない」が起きる）
3. **並べ替え機能をどうするか** — 3層にすると自由な並べ替えと矛盾する。
   層の中だけ並べ替え可にするか、機能ごと畳むか

### 先に測るとよいこと（判断材料）

「どのカードが実際に見られているか」はデータが無いと分からない。
H-1・H-2 を出したあと、**1〜2週間ふつうに使ってから**決めるのが確実だと思う。

---

## 4. テスト手順

1. `python3 -m http.server 8123` を立て `http://localhost:8123/index.html` を開く
   （`preview_start` のサーバーはサンドボックス制約で全404）
2. **並べ替えの検証**（H-1）
   ```javascript
   // タイマーを先頭にした状態を作る
   localStorage.setItem('gq_widget_order', JSON.stringify(
     ['timer-card','genre-card','daily-quest-card','mission-card','stats-strip','punch-card','calendar-panel']));
   // → リロードして、#app の子要素の並びと各カードの上端を確認する
   ```
   `はじめの一歩` と `今日の予定` は非表示のことが多いので、
   `style.display=''` で強制表示してから位置を測ること（隠れていると症状が出ない）
3. **ヘッダーの検証**（H-2）: 375px と 320px の両方。コントラストは計算して数値で示す
4. **新規ユーザー**: `localStorage.clear()` → リロードで起動と初期並び
5. コンソールエラーがゼロであること
6. `bash tools/bump_version.sh` → push後 `curl -s https://yoz400.github.io/growth-quest/ | grep v=guild-`

---

## 5. 迷ったら止まって報告すること

- `applyOrder` を変えたら、**並べ替えのドラッグ操作そのもの**が効かなくなった
  （`snapPositions` とアニメーションが DOM 順に依存している可能性がある）
- 席の数とカードの数が合わないケースが、想定より頻繁に起きる
- ロック中を 4.5:1 にしたら、**解放済みとの区別が付かなくなった**
- タップ領域を44pxにしたら、320px 端末でヘッダーが折り返した
- H-3 に手を出したくなった（→ §3 のとおりヨージの判断待ち。着手しないこと）

---

## 6. Codexへの依頼文（H-1・ヨージがコピペする）

```text
docs/spec_home_layers.md の H-1（並べ替えがカードを置き去りにする問題）を実装してください。

§1 の A案でお願いします。B案は採用しません（理由は §1 に書いてあります）。
席の数とカードの数がズレたときは、中途半端に動かさず並べ替えを諦めてください。

§1 の受け入れ基準を全部満たしてから、§4 のテスト手順で検証してください。
特に「はじめの一歩」と「今日の予定」は普段非表示なので、強制表示してから
位置を測らないと症状が出ません。

bash tools/bump_version.sh を実行し、日本語のコミットメッセージでコミットしてください。
§5 に当たったら、進めずに報告してください。

H-2 と H-3 はまだ着手しないでください。
```
